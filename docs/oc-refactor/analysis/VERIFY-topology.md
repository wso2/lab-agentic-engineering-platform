# VERIFY — Deployment Topology / WSO2 Cloud Readiness

Adversarial re-verification against source at `/Users/wso2/repos/labs-agentic-engineer`.
Date: 2026-05-24. Verdicts are independent of any prior analysis doc.

| Claim | Verdict | Evidence (file:line) | Note |
|---|---|---|---|
| 1. No Helm chart anywhere; platform tier has no packaged deployable artifact | CONFIRMED | `find . -name Chart.yaml` → no output (repo root) | No `Chart.yaml` exists. Platform services are only built via docker-compose. (OpenChoreo *itself* is installed from upstream OCI helm charts in `deployments/scripts/setup-openchoreo.sh:45`, but those are not in-repo and not the app-factory tier.) |
| 2. Platform services run via `deployments/docker-compose.yml` with LOCAL `build:` contexts (no pinned images) for postgres, asdlc-api(:9090), agents-service(:3400), git-service(:3300), console(:8090), smee-client | NEEDS-NUANCE | `deployments/docker-compose.yml:5,29,158,191,279,295` | Service list correct. BUT: postgres uses a **pinned image** `postgres:16-alpine` (`:6`) and smee-client uses a pinned image `deltaprojects/smee-client:latest` (`:280`) — these are NOT `build:`. The four first-party services DO use `build:` contexts: asdlc-api (`:30` ctx `../asdlc-service`, port `9090:9090` `:34`), agents-service (`:159` port `3400` `:163`), git-service (`:192` port `3300` `:195`), console (`:296` dockerfile `console/Dockerfile`, port `8090:3000` `:301`). So "no pinned images" is false for the two infra services. |
| 3. BFF→OC env vars are `PLATFORM_API_SERVICE_BASE_URL` (e.g. `http://k3d-openchoreo-serverlb:8080`) + `PLATFORM_API_SERVICE_HOST=api.openchoreo.localhost` | NEEDS-NUANCE | `deployments/docker-compose.yml:51-52` | Var **names are exactly correct**. `PLATFORM_API_SERVICE_BASE_URL` is passed through from `.env` (`${PLATFORM_API_SERVICE_BASE_URL}`), not literally set to the serverlb URL in compose; `PLATFORM_API_SERVICE_HOST` defaults to `api.openchoreo.localhost` (`:52`). The `k3d-openchoreo-serverlb:8080` form is the documented value (it is used literally for other vars e.g. `SERVICE_AUTH_TOKEN_URL` `:105`). |
| 4. OC extension resources (CCT service/web-application, ClusterTrait api-configuration, ClusterWorkflows dockerfile-builder/app-factory-coding-agent, Environment, DeploymentPipeline, ClusterAuthzRoleBindings) applied as INLINE `kubectl apply` heredocs in `setup-asdlc.sh`, all in `default` ns, not versioned charts, no plane separation | REFUTED (partially) | `deployments/scripts/setup-asdlc.sh:110,338,332,57,70,531,545,564` | Mixed. **Inline heredocs**: `ClusterComponentType service` (`:110-112`), `ClusterComponentType web-application` (`:338-342`), `Environment` (`:531`, ns `default` `:536`), `DeploymentPipeline` (`:545`, ns `default` `:550`), `ClusterAuthzRoleBinding` x2 (`:564,580`). **NOT inline** — applied from separate manifest files via `apply_with_retry`: `api-configuration` ClusterTrait ← `manifests/api-platform/api-configuration-trait.yaml` (`:332`); `dockerfile-builder` ClusterWorkflow ← `manifests/docker-build-workflow.yaml` (`:57`, def at `docker-build-workflow.yaml:643-645`); `app-factory-coding-agent` ClusterWorkflow ← `manifests/app-factory-coding-agent.yaml` (`:66-70`). "All landing in default ns" is also wrong: ClusterComponentType/ClusterTrait/ClusterWorkflow/ClusterAuthzRoleBinding are **cluster-scoped** (no namespace); only Environment + DeploymentPipeline use ns `default`. "Not versioned charts / no plane separation" is CONFIRMED (raw kubectl, no Helm). |
| 5. Per-service `workload.yaml` (asdlc/agents/git) are ASPIRATIONAL — not applied by any deploy path; declared ports/visibility may mismatch runtime (asdlc :9090 vs descriptor :8080) | NEEDS-NUANCE | `asdlc-service/workload.yaml:7`; `git-service/workload.yaml:7`; `agents/workload.yaml:7`; `.cicd/app-factory-api.yaml:22-23`; `deployments/docker-compose.yml:34` | Not applied by the **local** deploy path (no script/Go/TS reads/applies these three files — grep found only prompt text, dispatch error strings, and the build-workflow's `[ -f workload.yaml ]` check for the *generated user app*, not these). Port mismatch CONFIRMED: `asdlc-service/workload.yaml:7` declares `port: 8080` while runtime binds `9090` (compose `:34`, SERVER_PORT `:44`; note the asdlc default is also `8080` in `config_loader.go:28`, overridden by compose). HOWEVER "aspirational / no deploy path at all" needs nuance: `.cicd/app-factory-*.yaml` define a real WSO2 Cloud build pipeline (`type: docker`, ECR repo `app-factory-api`, `workloadPaths:` → `controlplane/.../app-factory-api/workload.yaml`) — these descriptors are the deployed-tier contract referenced by CI, just pointing at a copy in a *separate* controlplane repo, not applied locally. metadata names are `app-factory-*`, not the runtime container names. |
| 6. OpenBao runs DEV mode root token `root` (`values-openbao.yaml`), reachable at `host.docker.internal:8200`; git-service owns OpenBao access (not asdlc-api) | CONFIRMED | `deployments/single-cluster/values-openbao.yaml:18-21`; `deployments/docker-compose.yml:222-223,266`; `git-service/pkg/credentials/openbao_store.go` | `server.dev.enabled: true`, `devRootToken: root` (`values-openbao.yaml:19-21`). git-service env `OPENBAO_ADDR` defaults `http://host.docker.internal:8200`, `OPENBAO_TOKEN` defaults `root` (`docker-compose.yml:222-223`). Ownership: only git-service has a real OpenBao store (`git-service/pkg/credentials/openbao_store.go`, `app_token_minter.go`, `config_loader.go`); asdlc-service only references "openbao" in identifier validation (`asdlc-service/utils/validate/identifier.go`), no store and no OPENBAO_* env in its compose block. Reachability nuance: it is an **in-cluster** OpenBao Service exposed to the host via k3d NodePort 30820 (`k3d-local-config.yaml:38`, `8200:30820`), not a standalone container — the `host.docker.internal:8200` address is how compose services reach that NodePort. |
| 7. All four OC planes (Control/Data/Workflow/Observability) co-located in one k3d cluster (`k3d-local-config.yaml`, servers:1) | CONFIRMED | `deployments/k3d-local-config.yaml:6`; `deployments/scripts/setup-openchoreo.sh:45,69,199`; `deployments/scripts/setup-observability.sh` | `servers: 1`, `agents: 0` (`k3d-local-config.yaml:6-7`). Single k3d cluster `openchoreo` hosts control-plane (`setup-openchoreo.sh:45`), data-plane (`:69`), workflow-plane (`:199`), and observability plane (`setup-observability.sh`), each a helm release into its own namespace but one cluster/context. Port map in `k3d-local-config.yaml:10-39` exposes all four plane port ranges (8xxx/19xxx/10xxx/11xxx) off the single loadbalancer. |
| 8. git-service reaches the cluster via a HOST kubeconfig mount (not an in-cluster scoped ServiceAccount) | CONFIRMED | `deployments/docker-compose.yml:258,266`; `deployments/scripts/start.sh:106-129`; `git-service/clients/k8s/client.go:43` | git-service env `KUBECONFIG: /app/.kube/config` (`:258`), bind-mounted from `./.kube/config:ro` (`:266`). `start.sh` seeds that file from `k3d kubeconfig get --internal` (full cluster-admin creds, server rewritten to `k3d-openchoreo-server-0:6443`) (`start.sh:113-129`). git-service loads it via controller-runtime `config.GetConfig()` (`git-service/clients/k8s/client.go:43`), which reads `KUBECONFIG`. Not a scoped in-cluster ServiceAccount. |
| 9. Local dev uses smee.io relay forwarding GitHub webhooks to BFF `/webhooks/github` (smee-client service + target URL) | CONFIRMED | `deployments/docker-compose.yml:279-287`; `deployments/.env:50` | `smee-client` service runs `deltaprojects/smee-client` with `--url ${GITHUB_WEBHOOK_PROXY_URL} --target http://asdlc-api:9090/webhooks/github` (`:280,284`). `GITHUB_WEBHOOK_PROXY_URL=https://smee.io/GADXb56RHHozeW` (`.env:50`). git-service registers repo webhooks at the same smee URL (`docker-compose.yml:216`). |
| Extra: `deployments-v2/` directory exists in this repo (some doc references a WSO2 Cloud Flux/kustomize model) | REFUTED | `find . -type d -name deployments-v2` → no output | No `deployments-v2/` directory exists in this repo. The only mention is a code **comment** in `deployments/single-cluster/values-openbao.yaml:83` ("mirror v2's deployments-v2/wso2cloud-deployment/.../openbao.yaml") referencing a path in a *different* repo. |
