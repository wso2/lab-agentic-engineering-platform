# Adversarial Verification — ORCHESTRATION / WORKFLOW / BUILD / DEPLOY / LLM

Re-read the actual source under `/Users/wso2/repos/labs-agentic-engineer`. Each claim ruled
CONFIRMED / REFUTED / NEEDS-NUANCE with a `file:line` citation. No prior analysis doc trusted.

| # | Claim (abridged) | Verdict | Evidence (file:line) | Note |
|---|------------------|---------|----------------------|------|
| 1 | BFF talks to OC via generated oapi-codegen client in `clients/openchoreo/`, cached Thunder `client_credentials` Bearer + header `X-Use-OpenAPI: true`, 401→invalidate+retry | **CONFIRMED** | `clients/openchoreo/gen/client.gen.go:3` (oapi-codegen v2.7.0); `clients/openchoreo/transport.go:86` (`X-Use-OpenAPI`=`true`), `:88` (Bearer), `:67-76` (401→Invalidate+retry); `clients/oauth/token_provider.go:14,36-44,49-54,59` (cached client_credentials, Invalidate) | Header string verbatim `"X-Use-OpenAPI"` / `"true"`. 401 handler invalidates cached token and returns true (retry); auth editor re-runs `Token()` each attempt. All three confirmed. |
| 2 | Components created on GENERIC ClusterComponentTypes `deployment/service` / `deployment/web-application` (not agent-specific) | **CONFIRMED** | `services/design_service.go:25-32` (`ocEntrypoint`: web-app→`deployment/web-application`, default→`deployment/service`); used at `dispatch_service.go:633` (`Type: ocEntrypoint(...)`) | Generic OC types, no hardcoded agent type. Claim pointed at "~25"; exact match. |
| 3 | Coding-agent run AND build both dispatched as OC `WorkflowRun`s via OC client (`CreateWorkflowRun...`), referencing ClusterWorkflows `app-factory-coding-agent` / `dockerfile-builder` — not raw Jobs/Argo/kubectl | **CONFIRMED** | `component_client.go:1058` (`Name: "app-factory-coding-agent"`), `:1104` (`CreateWorkflowRunWithResponse`); build: `dispatch_service.go:638` (`Name: "dockerfile-builder"`) + `component_client.go:915-924` build WorkflowRun → `:1104`. Both via `createWorkflowRun` | No kubectl/Job/Argo authoring in BFF. Build dispatch ref is `component_client.go:887-924` (claim said ~1030 for coding-agent; coding-agent is at 1030). |
| 4 | Components created `AutoDeploy=true` (deploy delegated to OC AutoDeploy controller); BFF does NOT author Workload/ComponentRelease/ReleaseBinding for deploy (only writes RB `workloadOverrides` for env/config) | **CONFIRMED** | `dispatch_service.go:635` (`AutoDeploy: true`); `component_client.go:696` (`AutoDeploy: &req.AutoDeploy`); RB writes only update overrides: `:446` (`Container.Env`), `:509` (`Container.Files`), `:660` (TraitEnvironmentConfigs). No Create of Workload/ComponentRelease/ReleaseBinding anywhere in BFF Go (grep empty). | BFF only PATCHes existing RBs (GET→PUT), never creates the deploy chain. Confirmed. |
| 5 | `deployed` set on BUILD WorkflowRun success, NOT on `ReleaseBinding.Status.Conditions[Ready]` — task can be deployed before pod Ready | **CONFIRMED** | `build_watcher.go:213-225` (`classifyRun`: `ReasonWorkflowSucceeded`→`TaskEventBuildSucceeded`, gated only on `run.Completed`); `task_state.go:110` (`Building→Deployed` on `TaskEventBuildSucceeded`) | Terminal `deployed` keys off WorkflowRun completion (image built + workload posted), never RB Ready. A pod can still be pending → "deployed" is build-success, not pod-ready. |
| 6 | Build manifest: BUILD POD itself POSTs Workload to OC API (curl + client_credentials), hardcoded `oauth-client-secret` default with inline TODO | **CONFIRMED** | `deployments/manifests/docker-build-workflow.yaml:376-377` (TODO + `default: "openchoreo-workload-publisher-secret"`); `:480-484` (curl client_credentials token); `:503-508` (curl POST `/workloads` with Bearer) | TODO at line 375, default secret 376-377 (claim said ~376-377 ✓). POST workload at 502-508; PUT/merge fallback 515-575. All in-pod via curl. |
| 7 | Coding-agent WorkflowRun deliberately OMITS `openchoreo.dev/component` label to avoid OC ClusterWorkflow↔ComponentType allow-list (detached run) | **CONFIRMED** | `component_client.go:1020-1029` (comment: "deliberately NOT setting `openchoreo.dev/component`…allowed-workflow pair"); `:1042-1046` (labels use only `app-factory.*` keys, no `openchoreo.dev/component`) | Build run DOES set `openchoreo.dev/component` (`:912`); coding-agent run does not. Matches claim at ~1020. |
| 8 | LLM: both `agents/` and `remote-worker/` call `api.anthropic.com` DIRECTLY — no gateway/baseURL/proxy on any `createAnthropic`/Agent SDK call | **CONFIRMED** | `agents/src/shared/create-agent.ts:63`, `server/routes/{architect.ts:45,document-generation.ts:66,requirements-chat.ts:43,tech-lead.ts:86,238}` — all `createAnthropic({ apiKey })`, no `baseURL`; `remote-worker/src/lib/runner.ts:91-106` `query({...})` has no baseURL, `:52` `ANTHROPIC_API_KEY` from env | grep for `baseURL`/`proxy`/`ANTHROPIC_BASE_URL` in agents+remote-worker src = none. "gateway" hits are the WSO2 API Platform gateway for *deployed user services*, unrelated to LLM. No LLM gateway interposition. |
| 9 | "Model-agnostic" NOT implemented: single global model env (`AGENT_MODEL`, default a claude model), Anthropic hardcoded, no per-org/per-agent model/provider selection | **CONFIRMED** | `agents/src/shared/config.ts:2` (`model: process.env.AGENT_MODEL \|\| "claude-sonnet-4-5"`); all model uses are `anthropic(config.model)` (create-agent.ts:68, architect.ts:95, etc.); only `@ai-sdk/anthropic` imported (no openai/google provider). remote-worker uses Agent SDK default (claude) with no model override. | Single global env, one hardcoded provider. No per-org/per-agent selection path exists. |
| 10 | Build image written to WorkflowRun annotation `openchoreo.dev/workload` (same key AM uses) | **NEEDS-NUANCE** | `docker-build-workflow.yaml:602,610` (`.metadata.annotations["openchoreo.dev/workload"] = ($wl ...)`) | The annotation `openchoreo.dev/workload` is set, but its VALUE is the full Workload CR JSON (the `--rawfile wl` workload-cr.json), NOT the image string. The image is embedded inside that Workload's `spec.container.image`. So "the build image is written to the annotation" is imprecise — the *workload manifest* (containing the image) is written there. "Same key AM uses" not independently verifiable in this repo (no AM source present); plausible per the in-repo comment but uncited. |

## Summary

- **CONFIRMED:** 1, 2, 3, 4, 5, 6, 7, 8, 9
- **NEEDS-NUANCE:** 10
- **REFUTED:** none
